\section*{Direct Search-and-Replace Edits}

\subsection*{1. main.tex: Experiments overview appendix reference}

\noindent\textbf{Original}
\begin{verbatim}
Noisy-SGD details and supplementary image-noise results are provided in the
supplementary appendix, with Noisy-SGD in Appendix~I and
image-noise results in Appendices~I--L.
\end{verbatim}

\noindent\textbf{Replace with}
\begin{verbatim}
Noisy-SGD details and supplementary image-noise results are provided in the
supplementary appendix, with Noisy-SGD in Appendix~I and
image-noise results in Appendices~J--L.
\end{verbatim}

\subsection*{2. main.tex: Experimental setup image-noise appendix reference}

\noindent\textbf{Original}
\begin{verbatim}
Supplementary image-noise utility results are provided in the
supplementary appendix (Appendices~I--L), with detailed metric
comparison in Appendix~L, as supporting evidence only.
\end{verbatim}

\noindent\textbf{Replace with}
\begin{verbatim}
Supplementary image-noise utility results are provided in the
supplementary appendix (Appendices~J--L), with detailed metric
comparison in Appendix~L, as supporting evidence only.
\end{verbatim}

\subsection*{3. main.tex: Membership inference appendix reference}

\noindent\textbf{Original}
\begin{verbatim}
Supplementary image-noise utility results are provided in the
supplementary appendix (Appendices~I--L) and are included only as
supporting evidence, not as a central part of the main theorem-to-
mechanism evaluation pipeline.
\end{verbatim}

\noindent\textbf{Replace with}
\begin{verbatim}
Supplementary image-noise utility results are provided in the
supplementary appendix (Appendices~J--L) and are included only as
supporting evidence, not as a central part of the main theorem-to-
mechanism evaluation pipeline.
\end{verbatim}

\subsection*{4. main.tex: Table III synthetic image source}

\noindent\textbf{Original}
\begin{verbatim}
Synthetic 64 × 64 image & Image & Supplementary appendix (Appendix~I).
\end{verbatim}

\noindent\textbf{Replace with}
\begin{verbatim}
Synthetic 64 × 64 image & Image & Supplementary appendix (Appendices~J--L).
\end{verbatim}

\subsection*{5. appendix\_main.tex: remove duplicated paragraph in Appendix K}

\noindent\textbf{This paragraph currently appears twice. Keep one copy only and delete the duplicate.}

\noindent\textbf{Paragraph to keep once}
\begin{verbatim}
The left panel shows PSNR and the right panel shows MAE;
both are evaluated for $\varepsilon \in [0.1, 4]$ on a $64 \times 64$
synthetic image. The figure illustrates the $(\varepsilon,\delta)$-DP
utility--privacy tradeoff: smaller $\varepsilon$ implies more noise
(lower PSNR, higher MAE). At small $\varepsilon$ (e.g., 0.1) the
image is heavily perturbed; at $\varepsilon = 4$ the curves approach
a plateau. Laplace and Gaussian behave similarly; small
differences are due to the different noise distributions.
\end{verbatim}

\subsection*{6. appendix\_main.tex: convert inline bold headings to formal subsection headings}

\noindent\textbf{Original}
\begin{verbatim}
\textbf{Domain and range.} Let $D=\{x_1,\dots,x_n\}$ be a dataset over universe $\mathcal{U}$.
The mechanism $\mathcal{F}$ is a randomized map that outputs a synthetic record $y \in \mathcal{U}$.

\textbf{Input.} The mechanism takes as input the dataset $D$ together with tuning parameters
$k,t,\gamma,\varepsilon_0$ as used in Theorem~\ref{thm5}.

\textbf{Seed selection.} A seed $s$ is drawn uniformly at random from $D$:
\end{verbatim}

\noindent\textbf{Replace with}
\begin{verbatim}
\subsection*{Domain and range}
Let $D=\{x_1,\dots,x_n\}$ be a dataset over universe $\mathcal{U}$.
The mechanism $\mathcal{F}$ is a randomized map that outputs a synthetic record $y \in \mathcal{U}$.

\subsection*{Input}
The mechanism takes as input the dataset $D$ together with tuning parameters
$k,t,\gamma,\varepsilon_0$ as used in Theorem~\ref{thm5}.

\subsection*{Seed selection}
A seed $s$ is drawn uniformly at random from $D$:
\end{verbatim}

\subsection*{7. appendix\_main.tex: convert the remaining inline headings}

\noindent\textbf{Original}
\begin{verbatim}
\textbf{Candidate generation.} Given the seed $s$, a candidate $y$ is sampled from a
seed-dependent proposal distribution $p_s(\cdot)$ over $\mathcal{U}$.

\textbf{Partition rule.} For each candidate $y$, each seed $s$ is assigned a partition index $I_s(y)$.

\textbf{Privacy-test random variable.} Let $L$ be an integer-valued random variable,
independent of the seed-selection and candidate-sampling steps.

\textbf{Acceptance rule.} Given $(D,s,y)$ and the partition index $j=I_s(y)$, the privacy test
accepts the candidate with probability
\[
\mathrm{pt}(D,j,y)=\Pr\{L \ge k-|C_j(D,y)|\}.
\]

\textbf{Output and termination.} The mechanism $\mathcal{F}$ repeatedly performs the above
steps until a candidate is accepted, and outputs the accepted $y$.
\end{verbatim}

\noindent\textbf{Replace with}
\begin{verbatim}
\subsection*{Candidate generation}
Given the seed $s$, a candidate $y$ is sampled from a
seed-dependent proposal distribution $p_s(\cdot)$ over $\mathcal{U}$.

\subsection*{Partition rule}
For each candidate $y$, each seed $s$ is assigned a partition index $I_s(y)$.

\subsection*{Privacy-test random variable}
Let $L$ be an integer-valued random variable,
independent of the seed-selection and candidate-sampling steps.

\subsection*{Acceptance rule}
Given $(D,s,y)$ and the partition index $j=I_s(y)$, the privacy test
accepts the candidate with probability
\[
\mathrm{pt}(D,j,y)=\Pr\{L \ge k-|C_j(D,y)|\}.
\]

\subsection*{Output and termination}
The mechanism $\mathcal{F}$ repeatedly performs the above
steps until a candidate is accepted, and outputs the accepted $y$.
\end{verbatim}

\subsection*{8. Optional wording cleanup for the section opening}

\noindent\textbf{Original}
\begin{verbatim}
The main contributions of this section are entropy calibration
(Theorem~3, $\Delta H$ bound and calibrated histogram release)
and the synthetic-data mechanism $F$ (Theorem~4, Wiener kernel
and privacy test).
\end{verbatim}

\noindent\textbf{Replace with}
\begin{verbatim}
This section evaluates two main components of the paper:
entropy calibration (Theorem~3, $\Delta H$ bound and calibrated
histogram release) and the synthetic-data mechanism $F$
(Theorem~4, Wiener kernel and privacy test).
\end{verbatim}

\subsection*{9. Final global source check}

\noindent\textbf{Search for}
\begin{verbatim}
Appendices~I--L
Appendix~I).
Appendix~I and
The left panel shows PSNR and the right panel shows MAE;
\textbf{Domain and range.}
\textbf{Candidate generation.}
\textbf{Partition rule.}
H0a
??
\end{verbatim}

\noindent\textbf{After editing, verify}
\begin{verbatim}
Appendix~I      -> only for Noisy-SGD
Appendices~J--L -> only for image-noise material
the PSNR/MAE paragraph appears only once
mechanism F headings use \subsection* form
no H0a remains
no ?? remains
\end{verbatim}